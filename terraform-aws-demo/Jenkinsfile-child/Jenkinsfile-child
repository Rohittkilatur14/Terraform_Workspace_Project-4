pipeline {
    agent any

    parameters {
        choice(name: 'ENV', choices: ['dev', 'test', 'prod'])
        choice(name: 'ACTION', choices: ['apply', 'destroy'])
    }

    stages {
        stage('Echo Test') {
            steps {
                echo "Child pipeline running for ${ENV} with ${ACTION}"
            }
        }
    }
}
